<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Kerzz Teklif - #{{offer.no}}</title>
  <style>
    /* ── Reset & Base ── */
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      font-family: "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      font-size: 11px;
      line-height: 1.5;
      color: #1e293b;
      background: #fff;
    }

    /* ── Print Settings ── */
    @media print {
      @page {
        size: A4;
        margin: 12mm 15mm;
      }
      body { margin: 0; padding: 0; }
      .page-break { page-break-before: always; }
      .no-print { display: none !important; }
    }

    /* ── Logo ── */
    .logo {
      font-size: 32px;
      font-weight: 800;
      color: #0f172a;
      letter-spacing: 4px;
      margin-bottom: 8px;
    }

    .logo-subtitle {
      font-size: 10px;
      color: #64748b;
      letter-spacing: 2px;
      text-transform: uppercase;
    }

    /* ── Cover Header ── */
    .cover-header {
      text-align: center;
      padding: 20px 0 24px;
      border-bottom: 3px solid #0f172a;
      margin-bottom: 24px;
    }

    .cover-header h1 {
      font-size: 20px;
      font-weight: 700;
      color: #0f172a;
      letter-spacing: 1px;
      margin-top: 16px;
    }

    .cover-header .doc-type {
      font-size: 11px;
      color: #64748b;
      margin-top: 4px;
    }

    /* ── Reference Table ── */
    .ref-table-container {
      display: flex;
      justify-content: center;
      margin-bottom: 24px;
    }

    .ref-table {
      border-collapse: collapse;
      min-width: 320px;
      border: 1px solid #e2e8f0;
    }

    .ref-table td {
      padding: 10px 16px;
      border-bottom: 1px solid #e2e8f0;
    }

    .ref-table tr:last-child td {
      border-bottom: none;
    }

    .ref-table .ref-label {
      font-size: 10px;
      font-weight: 600;
      color: #64748b;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      background: #f8fafc;
      width: 140px;
    }

    .ref-table .ref-value {
      font-size: 12px;
      font-weight: 600;
      color: #0f172a;
    }

    /* ── Info Cards ── */
    .info-cards {
      display: flex;
      gap: 16px;
      margin-bottom: 24px;
    }

    .info-card {
      flex: 1;
      border: 1px solid #e2e8f0;
      border-radius: 6px;
      overflow: hidden;
    }

    .info-card-header {
      background: #0f172a;
      color: #fff;
      padding: 8px 14px;
      font-size: 10px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .info-card-body {
      padding: 12px 14px;
      background: #fff;
    }

    .info-card-body .primary {
      font-size: 13px;
      font-weight: 700;
      color: #0f172a;
      margin-bottom: 4px;
    }

    .info-card-body .secondary {
      font-size: 11px;
      color: #64748b;
      margin-bottom: 2px;
    }

    .info-card-body .detail {
      font-size: 10px;
      color: #94a3b8;
      line-height: 1.4;
    }

    /* ── Company Footer ── */
    .company-footer {
      border: 1px solid #e2e8f0;
      border-radius: 6px;
      padding: 14px 18px;
      margin-bottom: 24px;
      background: #f8fafc;
    }

    .company-footer-title {
      font-size: 10px;
      font-weight: 600;
      color: #0f172a;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-bottom: 8px;
      padding-bottom: 6px;
      border-bottom: 1px solid #e2e8f0;
    }

    .company-footer-grid {
      display: flex;
      gap: 24px;
    }

    .company-footer-col {
      flex: 1;
    }

    .company-footer-col .label {
      font-size: 9px;
      font-weight: 600;
      color: #94a3b8;
      text-transform: uppercase;
      margin-bottom: 2px;
    }

    .company-footer-col .value {
      font-size: 10px;
      color: #475569;
      line-height: 1.5;
    }

    /* ── Section ── */
    .section {
      margin-bottom: 18px;
    }

    .section-title {
      font-size: 11px;
      font-weight: 700;
      color: #0f172a;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      padding-bottom: 6px;
      border-bottom: 1px solid #cbd5e1;
      margin-bottom: 8px;
    }

    .section-body {
      font-size: 10px;
      color: #475569;
      line-height: 1.6;
    }

    .section-body p {
      margin-bottom: 4px;
    }

    /* ── Approval Box ── */
    .approval-box {
      border: 1px solid #cbd5e1;
      border-radius: 6px;
      padding: 16px;
      margin-top: 24px;
    }

    .approval-box .title {
      font-size: 11px;
      font-weight: 700;
      color: #0f172a;
      text-transform: uppercase;
      margin-bottom: 12px;
    }

    .approval-grid {
      display: flex;
      gap: 24px;
    }

    .approval-field {
      flex: 1;
      border-bottom: 1px solid #cbd5e1;
      padding-bottom: 8px;
    }

    .approval-field .label {
      font-size: 9px;
      font-weight: 600;
      color: #94a3b8;
      text-transform: uppercase;
      margin-bottom: 24px;
    }

    /* ── Acceptance Note ── */
    .acceptance-note {
      font-size: 9px;
      color: #64748b;
      margin-top: 16px;
      line-height: 1.5;
      font-style: italic;
    }

    /* ── Items Table ── */
    .items-section {
      margin-bottom: 28px;
    }

    .items-section-title {
      font-size: 12px;
      font-weight: 700;
      color: #0f172a;
      padding: 8px 0;
      border-bottom: 2px solid #0f172a;
      margin-bottom: 0;
    }

    .items-table {
      width: 100%;
      border-collapse: collapse;
    }

    .items-table thead th {
      font-size: 9px;
      font-weight: 600;
      color: #64748b;
      text-transform: uppercase;
      letter-spacing: 0.3px;
      padding: 8px 10px;
      border-bottom: 1px solid #e2e8f0;
      text-align: left;
    }

    .items-table thead th.right {
      text-align: right;
    }

    .items-table thead th.center {
      text-align: center;
    }

    .items-table tbody td {
      padding: 8px 10px;
      border-bottom: 1px solid #f1f5f9;
      vertical-align: top;
      font-size: 11px;
    }

    .items-table tbody tr:last-child td {
      border-bottom: 1px solid #e2e8f0;
    }

    .items-table .item-name {
      font-weight: 600;
      color: #0f172a;
    }

    .items-table .item-desc {
      font-size: 9px;
      color: #64748b;
      margin-top: 2px;
      line-height: 1.4;
    }

    .items-table .num {
      text-align: center;
      color: #64748b;
      font-weight: 500;
    }

    .items-table .amount {
      text-align: right;
      font-family: "SF Mono", "Cascadia Code", Consolas, monospace;
      font-size: 11px;
      white-space: nowrap;
    }

    /* ── Totals ── */
    .totals-row {
      display: flex;
      justify-content: flex-end;
      margin-top: 4px;
    }

    .totals-table {
      border-collapse: collapse;
      min-width: 280px;
    }

    .totals-table td {
      padding: 4px 10px;
      font-size: 11px;
    }

    .totals-table .label-cell {
      text-align: right;
      color: #64748b;
      font-weight: 500;
    }

    .totals-table .value-cell {
      text-align: right;
      font-family: "SF Mono", "Cascadia Code", Consolas, monospace;
      color: #0f172a;
      font-weight: 500;
    }

    .totals-table .grand-total .label-cell {
      font-weight: 700;
      color: #0f172a;
      font-size: 12px;
    }

    .totals-table .grand-total .value-cell {
      font-weight: 700;
      font-size: 12px;
      border-top: 2px solid #0f172a;
    }

    .totals-table .discount .value-cell {
      color: #dc2626;
    }

    /* Currency separator */
    .totals-table .currency-separator td {
      padding-top: 8px;
    }

    /* ── Print toolbar ── */
    .print-toolbar {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      background: #0f172a;
      color: #fff;
      padding: 10px 20px;
      display: flex;
      gap: 10px;
      align-items: center;
      z-index: 1000;
    }

    .print-toolbar button {
      background: #3b82f6;
      color: #fff;
      border: none;
      padding: 8px 16px;
      border-radius: 6px;
      font-size: 13px;
      font-weight: 600;
      cursor: pointer;
    }

    .print-toolbar button:hover {
      background: #2563eb;
    }

    .print-toolbar .info {
      font-size: 12px;
      color: #94a3b8;
      margin-left: auto;
    }

    @media print {
      .print-toolbar { display: none !important; }
      body { padding-top: 0 !important; }
    }

    @media screen {
      body { padding-top: 52px; }
    }
  </style>
</head>
<body>

  <!-- Yazdırma Toolbar (sadece ekranda) -->
  <div class="print-toolbar no-print">
    <button onclick="window.print()">Yazdır / PDF Kaydet</button>
    <span class="info">Teklif #{{offer.no}} — {{offer.customerName}}</span>
  </div>

  <!-- ═══════ SAYFA 1: KAPAK ═══════ -->
  
  <!-- Logo ve Başlık -->
  <div class="cover-header">
    <div class="logo">KERZZ</div>
    <div class="logo-subtitle">Veri Yazılım ve Danışmanlık A.Ş.</div>
    <h1>PROJE TEKLİFİ</h1>
    <div class="doc-type">[ EK-S-2 ]</div>
  </div>

  <!-- Referans Tablosu -->
  <div class="ref-table-container">
    <table class="ref-table">
      <tr>
        <td class="ref-label">Teklif No</td>
        <td class="ref-value">#{{offer.no}}</td>
      </tr>
      <tr>
        <td class="ref-label">Tarih</td>
        <td class="ref-value">{{formatDate offer.saleDate}}</td>
      </tr>
      <tr>
        <td class="ref-label">Geçerlilik Tarihi</td>
        <td class="ref-value">{{formatDate offer.validUntil}}</td>
      </tr>
      {{#if offer.pipelineRef}}
      <tr>
        <td class="ref-label">Referans Kodu</td>
        <td class="ref-value">{{offer.pipelineRef}}</td>
      </tr>
      {{/if}}
    </table>
  </div>

  <!-- Müşteri ve Satıcı Bilgileri -->
  <div class="info-cards">
    <!-- Müşteri Kartı -->
    <div class="info-card">
      <div class="info-card-header">Müşteri Bilgileri</div>
      <div class="info-card-body">
        <div class="primary">{{offer.customerName}}</div>
        {{#if offer.brandName}}
        <div class="secondary">{{offer.brandName}}</div>
        {{/if}}
        {{#if customer.address}}
        <div class="detail">{{customer.address}}</div>
        {{/if}}
        {{#if customer.taxNo}}
        <div class="detail">VKN: {{customer.taxNo}}{{#if customer.taxOffice}} - {{customer.taxOffice}}{{/if}}</div>
        {{/if}}
        {{#if customer.phone}}
        <div class="detail">Tel: {{customer.phone}}</div>
        {{/if}}
        {{#if customer.email}}
        <div class="detail">Email: {{customer.email}}</div>
        {{/if}}
      </div>
    </div>

    <!-- Satıcı Kartı -->
    <div class="info-card">
      <div class="info-card-header">Satış Temsilcisi</div>
      <div class="info-card-body">
        {{#if seller.name}}
        <div class="primary">{{seller.name}}</div>
        {{else}}
        <div class="primary">{{offer.sellerName}}</div>
        {{/if}}
        {{#if seller.phone}}
        <div class="detail">Tel: {{seller.phone}}</div>
        {{/if}}
        {{#if seller.email}}
        <div class="detail">Email: {{seller.email}}</div>
        {{/if}}
      </div>
    </div>
  </div>

  <!-- Şirket İletişim Bilgileri -->
  <div class="company-footer">
    <div class="company-footer-title">Kerzz İletişim</div>
    <div class="company-footer-grid">
      <div class="company-footer-col">
        <div class="label">İstanbul Ofis</div>
        <div class="value">[Adres bilgisi eklenecek]</div>
      </div>
      <div class="company-footer-col">
        <div class="label">Ankara Ofis</div>
        <div class="value">[Adres bilgisi eklenecek]</div>
      </div>
      <div class="company-footer-col">
        <div class="label">İletişim</div>
        <div class="value">
          Tel: 0850 224 95 94<br>
          Email: satis@veriyazilim.com.tr<br>
          Web: www.kerzz.com
        </div>
      </div>
    </div>
  </div>

  <!-- Finansal Açıklamalar -->
  <div class="section">
    <div class="section-title">FİNANSAL AÇIKLAMALAR</div>
    <div class="section-body">
      <p>Teklifimize KDV Dahil değildir. Belirtilen fiyatlara donanım, yazılım ve hizmet ürünlerinde %20 KDV, Yazar kasa donanım ürünlerinde %10 KDV ilave edilecektir.</p>
      <p>Teklifimiz, teklif tarihinden itibaren 15 gün için geçerlidir.</p>
      <p>Teklif kabulü sonrasında Yazılım Lisans, Destek Sözleşmesi ve/veya Kiralama sözleşmeleri imzalanmalıdır.</p>
      <p>Ürünlerin stok durumları ve kurulum tarihi ödeme sonrasında kesinleşir. Ödeme alınmadan kurulum planlamasına başlanmaz.</p>
      <p>Kerzz ofislerinin bulunmadığı yerlerdeki kurulumlarda ulaşım konaklama masrafları müşteriye aittir.</p>
      <p>Fiyat bilgisi size özel olarak ticari sır niteliğinde paylaşılmıştır. Üçüncü kişi ve kurumlarla paylaşmayınız.</p>
      <p>Döviz Fiyatları fatura tarihinde geçerli olan Garanti Bankası Efektif Satış Kuru üzerinden Türk Lirasına çevrilecektir.</p>
    </div>
  </div>

  <!-- Teknik Şartlar -->
  <div class="section">
    <div class="section-title">TEKNİK ŞARTLAR</div>
    <div class="section-body">
      <p>Cihazların kurulacağı yerlere Ethernet ve elektrik kabloları çekilmeli hazır hale getirilmelidir. Kablolama ve alt yapı işlemleri, Müşteri'nin sorumluluğundadır.</p>
      <p>Kablolama için kullanılması gerekli kablo tipi Cat.6 STP veya Cat.5 STP kablolardır.</p>
      <p>Data kablosu ve elektrik kablosu çekimlerinde aralarındaki mesafenin en az 60 cm olmasına dikkat edilmelidir.</p>
      <p>Tüm bilgisayar sistemi özel bir elektrik panelinden beslenmeli ve sigortalarının ayrı tutulması gerekmektedir.</p>
      <p>Merkez Şube bağlantılı sistemlerde MPLS, VPN bağlantı sistemleri önerilmektedir, Ancak VAE, ADSL ve G.HSDSL sistemleri ile de çalışmak mümkündür.</p>
      <p>Donanımlar Kerzz uyumluluğu test edilmiş ve gerekli modifikasyonlar yapılmış ürünlerdir.</p>
      <p>Donanım ürünleri 2 yıl süre ile üretici garantisindedir.</p>
      <p>Yazılım ürünleri her türlü üretim hatasına karşı garantilidir.</p>
    </div>
  </div>

  <!-- Çevre Şartları -->
  <div class="section">
    <div class="section-title">ÇEVRE ŞARTLARI</div>
    <div class="section-body">
      <p>Çalışma şartları için ortam sıcaklığı 15-40 C arası olmalıdır.</p>
      <p>Cihazların sıcaklık değerlerinin korunabilmesi için dört taraftan hava akışını sağlayacak bir düzen içinde korunmaları gerekmektedir.</p>
      <p>Cihazlar, ateş, mikrodalga, manyetik alan ve yüksek elektrikle çalışan aletlerin en az 60 cm uzağında olmalıdır.</p>
      <p>Elektrik hatları mutlaka topraklanmalıdır. Ayrıca U.P.S kullanımı ani voltaj değişikliklerinden etkilenmemek için şiddetle önerilmektedir.</p>
      <p>Uzaktan destek hizmetlerinin yerine getirilebilmesi için müşteri adreslerinde aktif ve çalışır durumda, Statik IP'ye sahip internet hatları gerekmektedir.</p>
    </div>
  </div>

  <!-- Teklif Onayı -->
  <div class="acceptance-note">
    Teklifimizi kabul ediyorsanız lütfen aşağıdaki alana Kaşe/İmza/Tarih atarak satis@veriyazilim.com.tr mail adresine veya 0850 224 95 94 nolu faksımıza gönderiniz.
  </div>

  <div class="approval-box">
    <div class="title">TEKLİF ONAYI</div>
    <div class="approval-grid">
      <div class="approval-field">
        <div class="label">YETKİLİ ADI SOYADI</div>
      </div>
      <div class="approval-field">
        <div class="label">TARİH</div>
      </div>
      <div class="approval-field">
        <div class="label">KAŞE - İMZA</div>
      </div>
    </div>
  </div>

  <!-- ═══════ SAYFA 2+: ÜRÜN TABLOLARI ═══════ -->
  <div class="page-break"></div>

  <!-- Donanım ve Diğer Ürünler -->
  {{#if hasProducts}}
  <div class="items-section">
    <div class="items-section-title">Donanım ve Diğer Ürünler</div>
    <table class="items-table">
      <thead>
        <tr>
          <th style="width:30px" class="center">#</th>
          <th>Açıklama</th>
          <th style="width:50px" class="center">Miktar</th>
          <th style="width:100px" class="right">Birim Fiyat</th>
          <th style="width:90px" class="right">İndirim</th>
          <th style="width:100px" class="right">Tutar</th>
        </tr>
      </thead>
      <tbody>
        {{#each offer.products}}
        <tr>
          <td class="num">{{increment @index}}</td>
          <td>
            <div class="item-name">{{this.name}}</div>
            {{#if this.description}}<div class="item-desc">{{this.description}}</div>{{/if}}
          </td>
          <td class="num">{{this.qty}} {{this.unit}}</td>
          <td class="amount">{{formatCurrency this.price this.currency}}</td>
          <td class="amount">{{formatCurrency this.discountTotal this.currency}}</td>
          <td class="amount">{{formatCurrency this.grandTotal this.currency}}</td>
        </tr>
        {{/each}}
      </tbody>
    </table>
    <div class="totals-row">
      <table class="totals-table">
        {{#each productsCurrencyTotals}}
        {{#if @index}}<tr class="currency-separator"><td colspan="2"></td></tr>{{/if}}
        <tr>
          <td class="label-cell">Toplam({{currencySymbol this.currency}})</td>
          <td class="value-cell">{{formatCurrency this.totalBeforeDiscount this.currency}}</td>
        </tr>
        {{#ifGt this.totalDiscount 0}}
        <tr class="discount">
          <td class="label-cell">İndirim({{currencySymbol this.currency}})</td>
          <td class="value-cell">{{formatCurrency this.totalDiscount this.currency}}</td>
        </tr>
        {{/ifGt}}
        <tr class="grand-total">
          <td class="label-cell">Genel Toplam({{currencySymbol this.currency}})</td>
          <td class="value-cell">{{formatCurrency this.totalGrand this.currency}}</td>
        </tr>
        {{/each}}
      </table>
    </div>
  </div>
  {{/if}}

  <!-- Yazılım Lisansları -->
  {{#if hasLicenses}}
  <div class="items-section">
    <div class="items-section-title">Yazılım Lisansları</div>
    <table class="items-table">
      <thead>
        <tr>
          <th style="width:30px" class="center">#</th>
          <th>Açıklama</th>
          <th style="width:50px" class="center">Miktar</th>
          <th style="width:100px" class="right">Birim Fiyat</th>
          <th style="width:90px" class="right">İndirim</th>
          <th style="width:100px" class="right">Tutar</th>
        </tr>
      </thead>
      <tbody>
        {{#each offer.licenses}}
        <tr>
          <td class="num">{{increment @index}}</td>
          <td>
            <div class="item-name">{{this.name}}</div>
            {{#if this.description}}<div class="item-desc">{{this.description}}</div>{{/if}}
          </td>
          <td class="num">{{this.qty}} {{this.unit}}</td>
          <td class="amount">{{formatCurrency this.price this.currency}}</td>
          <td class="amount">{{formatCurrency this.discountTotal this.currency}}</td>
          <td class="amount">{{formatCurrency this.grandTotal this.currency}}</td>
        </tr>
        {{/each}}
      </tbody>
    </table>
    <div class="totals-row">
      <table class="totals-table">
        {{#each licensesCurrencyTotals}}
        {{#if @index}}<tr class="currency-separator"><td colspan="2"></td></tr>{{/if}}
        <tr>
          <td class="label-cell">Toplam({{currencySymbol this.currency}})</td>
          <td class="value-cell">{{formatCurrency this.totalBeforeDiscount this.currency}}</td>
        </tr>
        {{#ifGt this.totalDiscount 0}}
        <tr class="discount">
          <td class="label-cell">İndirim({{currencySymbol this.currency}})</td>
          <td class="value-cell">{{formatCurrency this.totalDiscount this.currency}}</td>
        </tr>
        {{/ifGt}}
        <tr class="grand-total">
          <td class="label-cell">Genel Toplam({{currencySymbol this.currency}})</td>
          <td class="value-cell">{{formatCurrency this.totalGrand this.currency}}</td>
        </tr>
        {{/each}}
      </table>
    </div>
  </div>
  {{/if}}

  <!-- Kiralama ve Aylık Hizmetler -->
  {{#if hasRentals}}
  <div class="items-section">
    <div class="items-section-title">Kiralama ve Aylık Hizmetler</div>
    <table class="items-table">
      <thead>
        <tr>
          <th style="width:30px" class="center">#</th>
          <th>Açıklama</th>
          <th style="width:50px" class="center">Miktar</th>
          <th style="width:100px" class="right">Birim Fiyat</th>
          <th style="width:90px" class="right">İndirim</th>
          <th style="width:100px" class="right">Tutar</th>
        </tr>
      </thead>
      <tbody>
        {{#each offer.rentals}}
        <tr>
          <td class="num">{{increment @index}}</td>
          <td>
            <div class="item-name">{{this.name}}{{#if this.yearly}} (Yıllık){{/if}}</div>
            {{#if this.description}}<div class="item-desc">{{this.description}}</div>{{/if}}
          </td>
          <td class="num">{{this.qty}} {{this.unit}}</td>
          <td class="amount">{{formatCurrency this.price this.currency}}</td>
          <td class="amount">{{formatCurrency this.discountTotal this.currency}}</td>
          <td class="amount">{{formatCurrency this.grandTotal this.currency}}</td>
        </tr>
        {{/each}}
      </tbody>
    </table>
    <div class="totals-row">
      <table class="totals-table">
        {{#each rentalsCurrencyTotals}}
        {{#if @index}}<tr class="currency-separator"><td colspan="2"></td></tr>{{/if}}
        <tr>
          <td class="label-cell">Toplam({{currencySymbol this.currency}})</td>
          <td class="value-cell">{{formatCurrency this.totalBeforeDiscount this.currency}}</td>
        </tr>
        {{#ifGt this.totalDiscount 0}}
        <tr class="discount">
          <td class="label-cell">İndirim({{currencySymbol this.currency}})</td>
          <td class="value-cell">{{formatCurrency this.totalDiscount this.currency}}</td>
        </tr>
        {{/ifGt}}
        <tr class="grand-total">
          <td class="label-cell">Genel Toplam({{currencySymbol this.currency}})</td>
          <td class="value-cell">{{formatCurrency this.totalGrand this.currency}}</td>
        </tr>
        {{/each}}
      </table>
    </div>
  </div>
  {{/if}}

</body>
</html>
